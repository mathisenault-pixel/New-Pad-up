{
  "name": "padup",
  "version": "1.0.0",
  "description": "Pad'up - Application de réservation de terrains de padel",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android",
    "build:ios": "cap sync ios && cap open ios",
    "build:android": "cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
